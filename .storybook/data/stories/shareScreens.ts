import { shareScreens } from '../share-screens';
import defaultTheme from '../themes';

const shareScreensStory = {
    title: 'Share screens',
    metadata: {
        description: 'A story containing a few permutations of the Share screen.'
    },
    theme: defaultTheme,
    components: shareScreens
};

export default shareScreensStory;
