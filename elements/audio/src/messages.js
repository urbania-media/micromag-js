import { defineMessages } from 'react-intl';

const messages = defineMessages({});

export const schemas = defineMessages({
    schemaTitle: {
        id: 'schema.title',
        defaultMessage: 'Audio element',
    },
    url: {
        id: 'schema.properties.url',
        defaultMessage: 'URL',
    },
});

export default messages;
