import { defineMessages } from 'react-intl';

const messages = defineMessages({});

export const schemas = defineMessages({
    schemaTitle: {
        id: 'schema.title',
        defaultMessage: 'Link element',
    },
    url: {
        id: 'schema.url',
        defaultMessage: 'URL',
    },
});

export default messages;
